<% if flash[:notice] %>
      <p>
        <%= flash[:notice] %>
      </p>
<% end %>

<p>
  <strong>Title:</strong>
</p>
<p><%= @book.title %></p>

<p>
  <strong>Body:</strong>
</p>
<p><%= @book.body %></p>

<%= link_to "edit", edit_book_path(@book.id), class: "edit_#{book.id}" %> |
<%= link_to "back", new_book_path(@book.id), class: "back" %>